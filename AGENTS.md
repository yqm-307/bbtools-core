# AGENTS.md — bbtools-core

## 定位与职责

- 本仓是通用 C++ 基础库，包含 `bbt/core/` 下的工具/类型及 `bbt/pollevent/` 事件基础；向上供 coroutine 和统一 `bbtools-infra` 消费；framework 通过 infra 获取服务基础设施。
- 不反向依赖 bbtools-coroutine、bbtools-infra、bbt-framework 或应用；不承载协程调度语义、HTTP 协议与业务模型。
- 本仓拥有基础能力的公共契约；影响 coroutine 时核对其运行时契约与消费者，不能把上游测试通过当作下游兼容。
- 当前 CMake 使用 C++17，代码/测试是实现事实；平台演进目标不等于已经支持。相关跨平台工作复用 bbtools-coroutine #339，不重复立项。

## 权威与协作

- 本文件是跨工具入口，README 是用户说明，GitHub Issue/PR/Review 是需求、决策、进度与验收记录；冲突应明确指出，不静默修改已有契约。
- 先读取目标 Issue、实际分支、源码与消费者再修改；普通任务最小充分实现，不自动进入 SDD。
- 每项任务一位实现负责人；并行写入使用独立工作区，记录分支、基线、范围与下一步。公共接口由一个负责人协调，上下游固定已验收版本后集成。
- 当前默认分支为 `master`。现有 workflow 的 push/PR 分支过滤为 `main`，不因此假定 PR 自动检查有效；本轮不改分支或 CI，后续需另行确认修正。
- 不复制团队 SOUL/Skill 或其他仓库全部规则；项目入口不依赖本机绝对路径或聊天记录。

## 工程规范

- 第三方协议/客户端能力统一由 bbtools-infra 接入；core 仅在基础原语确有需要时新增依赖。优先标准库/Boost，必要新依赖先核查维护活跃度、架构/平台/许可/安全与退出路径；不因统一工具层而改变 core 自身契约。

- 中文文档与交付，命令、标识符和错误原文保留；沿用邻近模块命名、返回码和布局，不为风格统一大改。
- 优先现有能力，所有权、并发与事件时序必须明确；不新增未经验证的跨线程约定。
- 新源码/测试须纳入实际 CMake/CTest；接口变更检查直接消费者，兼容性破坏先确认。
- 长期规范与必要验证摘要可入仓；原始日志、缓存、构建产物、会话、凭据与私有路径不入仓。先检查忽略规则，不执行无关清理。

## 统一编码与架构约束

以下六条为三仓（bbtools-core / bbtools-coroutine / bbtools-infra）统一约束，作为代码审查门禁；本仓按拆分计划退场前同样遵守。

1. **namespace 不超过三层**：形态为 `bbt::{大模块}::{小模块}`；禁止新增四层及以上嵌套，出现更深层级优先重新划分模块。
2. **Boost 1.90 优先**：标准库与 Boost 已有成熟上位能力时禁止重复造轮子；保留自研实现须有可复核的差异理由（语义、性能、平台或生命周期）。
3. **C++17**：新增与迁移代码以 C++17 为基线，不静默提升标准。
4. **模板适度**：用模板提升类型安全与复用；禁止为技巧性引入难理解、难诊断、难维护的元编程。
5. **基础库尽量 header-only**：纯算法、类型工具、无状态小组件优先 header-only；有稳定 ABI、重状态、I/O、平台隔离或明显编译成本的模块可保留编译单元并说明理由。
6. **依赖方向**：`bbtools-infra → bbtools-coroutine`，禁止 `bbtools-coroutine → bbtools-infra`；core 不反向依赖任何上层仓。

## 验证入口与限制

- 文档修改：`git diff --check`，核对路径、UTF-8 和目标范围。
- 既有 CI 编译脚本：`shell/workflow/unit_test/compile_code.sh`；它会删除传入仓库的 `build/` 再运行 CMake 和 make。不是无副作用探针，使用前确认该目录归属及删除授权，不在共享工作区盲跑。
- 测试由 `unit_test/CMakeLists.txt` 注册，既有 CI 在 `build/unit_test/` 执行 `ctest`；本次初始化只静态核对入口，没有构建或测试通过结论。
- `shell/build.sh` 包含系统目录写入和 `sudo`，只在明确安装授权后使用，不作为默认本地验证入口。
- 实现任务须有可复现的构建与相关测试证据，无新增警告；公共接口改动由下游运行必要集成验证。缺依赖或工具链如实说明，不自动系统安装。
- 本地验证边界：必走冒烟 + 本次开发功能的单测 + 直接耦合功能的单测，三者全过；本地不跑全量 `ctest`，全量走 PR 的 CI。同机多 agent 并行时：新 configure 加 `-DCMAKE_CXX_COMPILER_LAUNCHER=ccache`；本地并发取 `JOBS=$(( $(nproc) / 4 ))`（最小2，8核→2），增量用 `--target <t> --parallel $JOBS`；确需本地全量时串行（`flock /tmp/bbt-build.lock`，一次一个，`--parallel $(( $(nproc) / 2 ))`，8核→4）；禁止 bare `--parallel` / bare `ninja` / `make -j$(nproc)`。

## 独立接续与同机协作

- 用户可直接与本仓 Agent 确认具体任务，不必经 Alice 转交或审批；跨仓契约变化与相关拥有者协调，不能代改其他仓。用户明确消息或携带其授权范围的任务说明可作为输入，Issue 存在不等于授权；本节不放宽既有提交、依赖和安全门禁。
- 开工在任务说明中记清：目标/非目标、实现负责人、仓库与基线完整 SHA、实际工作区/分支、允许写入、依赖产物及就绪判据、验证和交付位置。可复用 Issue/PR/交接正文，不强制新系统或固定模板。普通可查信息先查；缺方向或权限才问用户。
- 写任务各用独立工作区/分支、build 和依赖目录；不共享 Git 索引、测试数据或未声明的进程。复用上游须核对源码 SHA、模块及实际链接的依赖路径和对应验证；tag 可用但须解析到 SHA，`/usr/local` 已安装不等于版本已固定。不自动系统安装或覆盖共享前缀；现有构建暂不能隔离时先记录阻塞，不擅改构建入口。
- 测试优先动态端口与自有进程；需要固定共享服务时，在任务中声明资源、复用/互斥和清理边界，不终止他人进程。保留已有脏改动；只读不要求全仓洁净，写任务不裹入他人改动。
- 同机新 worktree 和云端 clone 都不会带入未提交规范。开工核对实际拿到的规范版本；未发布时由授权交接者提供明确候选快照/差异及适用范围，不把旧暂存版当最新版。长期结论保留于本仓规定文档/PR，原始 `.state`/会话不作唯一公共依据；不要求读取 Alice 历史或私有记忆。

- 接收上层需求先看 [bbtools-infra #1](https://github.com/yqm-307/bbtools-infra/issues/1) 关联的具体缺口，不把主 PRD 当本仓实现任务。缺口须带双方 SHA、最小复现、期望契约位置、真实命令/失败证据；先与既有任务去重，无缺口交核验结论。
- core 原语契约归本仓；协议适配政策归 infra 的 `docs/decisions/0001-unified-infra.md` / 主 Issue。本仓只摘要其边界，不反向依赖 infra。交给 coroutine/infra 的版本记录至少有源码完整 SHA、构建/验证命令及证据；消费者记录实际依赖路径并复验，不以本仓单测代替下游兼容。

## 任务入口

- 本仓职责与接入核验：https://github.com/yqm-307/bbtools-core/issues/11
- 实施前核对规范是否已发布及任务授权，逐次进度留在 Issue/PR。

## 权限、提交与交接

- 在用户授权范围内阅读、实现和运行非破坏性本地验证；新增依赖、CI/构建脚本、公共接口破坏与系统安装先确认。
- 经用户授权后 commit/push，代码走 PR，不直推默认分支、不自行合并或发布。关键公共契约与并发变更须独立审查，不绕过 required checks。
- 提交仅包含目标文件，保留用户已有改动。中文提交标题使用 `<type>(<scope>): <说明>`，Agent author 显式设为 `agent <agent@users.noreply.github.com>`。
- Issue 创建不等于启动全部实施；不部署、不改生产、不执行付费调用或破坏性清理。
- 交接包含实际版本、变更、验证结果、未覆盖项、PR/产物与下一步；远端写入后回读。没有环境/授权/依赖时报告 BLOCKED。
